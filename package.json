{
  "name": "viz-content-model",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world",
    "clean": "bsb -clean-world",
    "start:bsb": "bsb -make-world -w -ws _ ",
    "start:server": "moduleserve ./ --port 8000 --spa",
    "start": "concurrently \"yarn start:bsb\" \"yarn start:server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "BuckleScript",
    "ReasonReact",
    "reason-react"
  ],
  "author": "lucent1090",
  "license": "MIT",
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "reason-react": ">=0.7.0"
  },
  "devDependencies": {
    "bs-platform": "^7.2.2",
    "concurrently": "^5.1.0",
    "moduleserve": "^0.9.0"
  }
}
